Aplikasi Absensi (PHP Native) - Generated
Folder: /mnt/data/absensi_app
Instructions:
- Place this project in your PHP-enabled server (e.g., XAMPP htdocs).
- Create a database and import `sql/schema.sql`.
- Update `config/config.php` with your DB credentials.
- Ensure folders `uploads/absensi` and `uploads/profil` are writable.
